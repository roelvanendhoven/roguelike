# roguelike
A roguelike written using the python-tcod library.

Install it in a python vitual environment with Python 3.5+, newest prefered. Then pip install the requirements.txt file like so: `pip install -r requirements.txt`
